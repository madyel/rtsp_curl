language: python

python:
    - "3.8"

install:
  - pip install -r requirements.txt
  - pip install coverage

script: true
  - nosetests --with-coverage --coverage report

after_script:
  - coveralls

after_success:
- codecov